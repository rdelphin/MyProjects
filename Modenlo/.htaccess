RewriteEngine On

# Keep real files/folders working
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Leave API paths untouched
RewriteRule ^api/ - [L]

# Send everything else to the SPA entry point
RewriteRule ^ index.html [L]
